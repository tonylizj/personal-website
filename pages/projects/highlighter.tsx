import ProjectDetails from '../../components/ProjectDetails';

const highlighter = () => (
  <ProjectDetails
    title="highlighter"
    subtitle="Node.js + Express API and Discord bot that syntax highlights code."
    text="Some things about highlighter"
  />
);

export default highlighter;
